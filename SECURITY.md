# 安全策略

## 适用范围

安全修复优先适用于 `main` 的最新提交。项目尚未发布带长期维护承诺的稳定版本；使用旧二进制的部署应在修复发布后尽快升级。

## 威胁模型与部署边界

PaiHuo 是**受信任管理员使用的宿主机控制台**：创建任务、安装 agent、目录选择、技能导入和任务执行都可能读写运行服务的 Linux 用户可访问的文件，并执行该用户有权执行的命令。它不提供多租户隔离、细粒度授权或对不可信任务提示词的沙箱。

安全部署至少应做到：

- 使用高熵 `PAIHUO_TOKEN`。公开监听地址未配置令牌时，程序会拒绝启动。
- 默认监听 `127.0.0.1`；远程访问应通过受信任网络或配置 TLS 的反向代理。TLS 在代理处终止时，启动参数增加 `--secure-cookie`。
- 不要把服务、数据库、任务日志、agent 凭据或工作目录暴露给不受信任的用户。
- 以专用、最小权限的系统用户运行服务；只把需要的项目目录和凭据交给该用户。
- 定期备份 `paihuo.db`，并在升级前验证可以恢复。

将受攻击者控制的提示词、仓库或 agent 插件交给拥有生产权限的服务账户，属于高风险操作；请先在隔离环境中评估。

## 报告漏洞

请不要在公开 issue、讨论区、提交信息或 pull request 中披露安全漏洞、可利用细节、访问令牌或私有数据。

优先使用仓库的 GitHub **Report a vulnerability** 私密报告入口（`Security` → `Advisories`）。报告中请包含：

- 受影响的提交、版本或部署方式；
- 可复现步骤与影响范围；
- 最小化的概念验证；
- 建议的缓解方式（如有）。

若该入口尚未启用，请先联系仓库维护者取得私密沟通渠道；不要改为公开披露。维护者会确认报告、评估影响，并在修复准备好后协调披露。

## 处理原则

- 修复应附带不会暴露漏洞细节的回归测试。
- 凭据泄露、任意命令执行、认证绕过、路径逃逸和数据破坏均应按高优先级处理。
- 安全修复发布后，应在变更说明中交代受影响范围、升级方式和必要的令牌/凭据轮换步骤。
