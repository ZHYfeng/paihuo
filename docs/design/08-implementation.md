# 实现路线图

> 状态：已细化 · 更新：2026-08-08
> 汇总所有设计文档的实施计划。依赖关系自上而下，各阶段独立可交付、可验证。
> **实现状态（2026-08-08 完成）：阶段 A/B/C 全部、D 全部、E 部分（R5/markdown + N2-L1）、阶段 F（S5）全部。**
> 剩余：E 的 R6（多轮 diff 对比，需后端快照）、R7（失败定位）、N2-L2/L3（agent 辅助推荐技能）。

## 完成清单（2026-08-08）

| 里程碑 | 内容 | 验证 |
|---|---|---|
| S-1a | sessions 表 + CRUD + 状态机 | 单测 + curl API ✅ |
| S-1b | rpcproc 进程封装 + JSONL 解析 + 命令通道 | 真实 pi 冒烟 ✅ |
| S-1c | 管理器集成：槽位共用 + SSE + 全部 API + 交付桥接 | 端到端（建→聊→挂→恢→交付→任务执行）✅ |
| S-1d | 恢复/崩溃处理 + Recover + pi 版本兼容 | 崩溃自动挂起 + 重启恢复 ✅ |
| S-2 | 会话页 lit 组件（列表/新建/详情/输入/终端面板） | playwright 渲染冒烟 ✅ |
| S-3 | 消息流（transcript + SSE 增量 + 工具卡片 + 思考块） | 真实对话流式渲染 ✅ |
| S-4 | 交付闭环（worktree 复用 + 任务/会话回链） | 交付任务在会话分支执行 ✅ |
| D-1/R1 | diff 审查器（文件列表/行号/折叠/常显） | 构建+渲染 ✅ |
| R2 | 驳回意见时间线 + body markdown | 解析函数 + 渲染 ✅ |
| R3 | 日志错误过滤 | toggle 过滤 ✅ |
| R4 | 审批联动（diff 内批准/驳回） | API 接通 ✅ |
| R5 | 任务说明 markdown | md 渲染 ✅ |
| N2-L1 | 角色技能按 tags 选择 + 全选筛选 | 构建 ✅ |
| S5 | codex/claude 终端式会话（tmux 通道 + xterm 面板） | codex 全流程（启/输/挂/恢）✅ |

## 依赖总览

```
阶段 A 会话后端（S-1a→S-1b→S-1c→S-1d）
   │
   ├─→ 阶段 B 会话前端（列表/详情/消息流/输入）
   │        │
   │        └─→ 阶段 C 交付闭环（交付桥接 + 回链）
   │
   └─→ 阶段 D 任务详情升级（diff 审查器 R1/R4 + 日志 R3 + 驳回时间线 R2）
             │
             └─→ 阶段 E 体验补全（markdown R5 / 多轮对比 R6 / 失败定位 R7 / skills N2）
                        │
                        └─→ 阶段 F codex/claude 降级会话（S5）
                                    │
                                    └─→ 远期 N4 pi 核心编排
```

## 阶段 A：会话后端（依赖 03 文档）

| 里程碑 | 内容 | 验证 |
|---|---|---|
| S-1a | sessions 表 + CRUD + 状态机（无进程） | 纯后端单测；curl API |
| S-1b | rpcproc 进程封装 + JSONL 解析 + 命令通道 | 单测 + 真实 pi 冒烟（spawn→prompt→事件） |
| S-1c | 管理器集成：槽位共用 + SSE 事件 + 全部 API + 交付桥接 | 端到端：建会话→对话→挂起→恢复→交付→任务跑通 |
| S-1d | 恢复/崩溃处理 + 挂起清理 + pi 版本门槛 | 杀进程恢复测试 |

**前置**：`internal/exec/slots.go` 槽位提取（与任务共用）。

## 阶段 B：会话前端（依赖 04/05 文档）

| 里程碑 | 内容 | 验证 |
|---|---|---|
| S-2a | lit 引入 + 构建拆分 + `#/sessions` 页面骨架 + 会话列表/新建 | 列表 CRUD 可用 |
| S-2b | 会话详情：header + transcript 全量渲染 + 消息组件（user/assistant/事件） | 打开旧会话看到完整时间线 |
| S-2c | 流式渲染：SSE 增量 + 工具卡片 + bash 卡片 + 输入区（队列/中止） | 实时对话体验 |
| S-2d | 虚拟滚动 + 移动端 + 侧栏属性 | 千条消息流畅 |

**前置**：阶段 A 的 S-1b（有真实进程可对话）。

## 阶段 C：交付闭环（依赖 06 文档）

- 交付 API + worktree 复用改造（Ensure 支持 session_id）
- 前端交付弹窗 + 会话冻结 + 任务/会话双向回链
- **验证**：端到端——对话→交付→合并任务成功整合分支

## 阶段 D：任务详情升级（R1-R4）

| 里程碑 | 内容 | 依赖 |
|---|---|---|
| D-1 | diff 解析器（纯函数+单测）+ `<ph-task-diff>`（文件列表/行号/折叠/导航） | 无（后端零改动，独立交付） |
| D-2 | 审查-审批联动（diff 内操作条）+ 驳回意见时间线（review_note 结构化） | 后端小改（意见独立字段） |
| D-3 | 日志虚拟滚动 + 错误过滤 + 失败定位 | 无 |

**注意**：D-1 与阶段 A/B 无依赖，**可并行或先行**（价值最大、成本最低）。

## 阶段 E：体验补全

- R5 任务说明 markdown 渲染（body 纯净后）
- R6 多轮 diff 对比（后端记录每轮快照，需数据模型评估）
- R7 失败快速定位
- N2 skills 三级加载：tags 选择器（L1）→ agent 推荐（L2，用 pi RPC 批处理）

## 阶段 F：codex/claude 降级会话（依赖 07 文档）

- 会话执行器增加 tmux 通道（复用现有交互任务机制）
- `<ph-term-panel>` 降级视图
- CLI 会话恢复（--continue 语义）验证

## 远期：N4 pi 核心编排

- 角色模型解耦"一个角色=一个 CLI"
- 以 pi（RPC/嵌入）为调度核心的 agent 间编排
- 自动合并任务的泛化

## 建议执行顺序（考虑价值/成本/依赖）

```
1. D-1  diff 审查器          ← 甜蜜区最大短板、零依赖、纯前端
2. 阶段 A 会话后端 S-1a~S-1c  ← 会话地基（S-1d 可后补）
3. 阶段 B + C 会话前端与交付   ← 核心体验闭环
4. S-1d + 阶段 D 剩余 + E      ← 健壮性与体验补全
5. 阶段 F 降级会话             ← 覆盖 codex/claude 用户
```
